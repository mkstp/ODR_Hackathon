\documentclass[12pt, letterpaper, parskip=half]{scrartcl}

%%% FONTS %%%
\usepackage{fontspec}
\setmainfont{Palatino}
\setmonofont{Menlo}[Scale=0.85]

% Keep serif fonts in headings (KOMA default is sans)
\addtokomafont{disposition}{\rmfamily}
\setkomafont{section}{\large\bfseries}
\setkomafont{subsection}{\normalsize\bfseries}
\setkomafont{subsubsection}{\normalsize\itshape}
\RedeclareSectionCommand[beforeskip=1.5ex plus 0.5ex minus 0.2ex, afterskip=0.5ex]{section}
\RedeclareSectionCommand[beforeskip=1.2ex plus 0.4ex minus 0.2ex, afterskip=0.4ex]{subsection}
\RedeclareSectionCommand[beforeskip=1ex plus 0.3ex, afterskip=0.3ex]{subsubsection}
\setcounter{secnumdepth}{0}

%%% PAGE GEOMETRY %%%
\usepackage[top=1in, bottom=1in, left=1.25in, right=1.25in]{geometry}

%%% TYPOGRAPHY %%%
\usepackage{microtype}
\usepackage{setspace}
\setstretch{1.2}
\setlength{\emergencystretch}{3em}

%%% COLOR %%%
\usepackage{xcolor}

%%% HYPERLINKS %%%
\usepackage[hidelinks, bookmarks=true, bookmarksopen=true]{hyperref}

%%% HEADERS & FOOTERS %%%
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
$if(title)$\fancyhead[L]{\small\slshape $title$}$endif$
\fancyhead[R]{\small\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\fancypagestyle{plain}{
  \fancyhf{}
  \fancyfoot[C]{\small\thepage}
  \renewcommand{\headrulewidth}{0pt}
}

%%% TABLES %%%
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{array}
\usepackage{calc}
\providecommand{\real}[1]{#1}

%%% PANDOC HELPERS %%%
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

%%% GRAPHICS %%%
\usepackage{graphicx}
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\makeatother
\setkeys{Gin}{width=\maxwidth,keepaspectratio}

%%% CSL REFERENCES (pandoc --citeproc) %%%
$if(csl-refs)$
\newlength{\cslhangindent}
\setlength{\cslhangindent}{1.5em}
\newlength{\csllabelwidth}
\setlength{\csllabelwidth}{3em}
\newenvironment{CSLReferences}[2]
 {\begin{list}{}%
  {\setlength{\leftmargin}{\cslhangindent}%
   \setlength{\itemindent}{\dimexpr\cslhangindent*-1\relax}%
   \setlength{\parsep}{\parskip}%
   \setlength{\topsep}{0pt}%
   \setlength{\itemsep}{#1\baselineskip}}}%
 {\end{list}}
\newcommand{\CSLBlock}[1]{\hfill\break#1\hfill\break}
\newcommand{\CSLLeftMargin}[1]{\parbox[t]{\csllabelwidth}{\strut #1\strut}}
\newcommand{\CSLRightInline}[1]{\parbox[t]{\dimexpr\linewidth - \csllabelwidth\relax}{\strut #1\strut}}
\newcommand{\CSLIndent}[1]{\hspace{\cslhangindent}#1}
$endif$

%%% TITLE BLOCK %%%
$if(title)$
\title{$title$$if(subtitle)$\\\medskip\large\normalfont $subtitle$$endif$}
$endif$
$if(author)$
\author{$for(author)$$author$$sep$ \and $endfor$}
$endif$
\date{$if(date)$$date$$else$$endif$}

%%%---DOCUMENT---%%%

\begin{document}

$if(title)$
\maketitle
\thispagestyle{plain}
$endif$

$if(abstract)$
\begin{abstract}
\noindent $abstract$
\end{abstract}
\vspace{1em}
$endif$

$if(toc)$
\tableofcontents
\newpage
$endif$

$body$

\end{document}
